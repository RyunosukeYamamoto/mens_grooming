<header class="mb-4" style="background-color:blue;">
  <% if logged_in? %>
    <p>ログイン完了ユーザ: <%= current_user.name %></p>
    <%= link_to 'Logout', logout_path, method: :delete %>
  <% else %>
    <p>ログインしていません</p>
  <% end %>
</header>